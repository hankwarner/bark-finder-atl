const User = require('../models').User
const Park = require('../models').Park

module.exports = {

    // async getReview(callback) {
    //     try {
    //         let reviews = await Review.

    //         return callback(null, reviews)
            
    //     } catch(error) {
    //         console.log(error)
    //         callback(error)
    //     }
    // }
}